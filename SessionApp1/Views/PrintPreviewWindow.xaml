<Window x:Class="SessionApp1.Views.PrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Предварительный просмотр печати" Height="600" Width="800"
        WindowStartupLocation="CenterScreen" Background="#FFE0D5CA">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Контейнер для содержимого печати -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <Border BorderBrush="#B5D5CA" BorderThickness="1" Background="White" Padding="10">
                <Grid x:Name="PrintContentContainer">
                    <!-- Содержимое для печати будет добавлено программно -->
                </Grid>
            </Border>
        </ScrollViewer>
        
        <!-- Панель кнопок -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="PrintButton" Content="Печать" Width="120" Height="30" Margin="0,0,10,0" Click="PrintButton_Click"/>
            <Button x:Name="CloseButton" Content="Закрыть" Width="120" Height="30" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>