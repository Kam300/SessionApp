<Page x:Class="SessionApp1.Views.DirectorScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Экран директора"
      Background="{StaticResource BackgroundColor}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" Padding="20,10">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <Image Source="/Logo/logo-04.jpg" Width="60" Height="40" Margin="0,0,20,0"/>
                    <TextBlock x:Name="WelcomeText" FontSize="18" FontWeight="Bold" 
                               Foreground="{StaticResource TextDarkColor}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="LogoutButton" Content="Выход" 
                        Style="{StaticResource AccentButton}"
                        Click="LogoutButton_Click" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Навигационная панель -->
        <Border Grid.Row="1" Background="{StaticResource SecondaryLightColor}" Padding="20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="ProductsButton" Content="Изделия" 
                        Style="{StaticResource StandardButton}"
                        Click="ProductsButton_Click" Margin="0,0,10,0"/>
                <Button x:Name="StockReportButton" Content="Остатки материалов" 
                        Style="{StaticResource StandardButton}"
                        Click="StockReportButton_Click" Margin="0,0,10,0"/>
                <Button x:Name="MovementReportButton" Content="Движение материалов" 
                        Style="{StaticResource StandardButton}"
                        Click="MovementReportButton_Click" Margin="0,0,10,0"/>
                <Button x:Name="OrderListButton" Content="Список заказов" 
                        Style="{StaticResource StandardButton}"
                        Click="OrderListButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Основной контент -->
        <Frame x:Name="ContentFrame" Grid.Row="2" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>